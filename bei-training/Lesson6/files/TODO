TODO
# process the remaining files in the zip
# refactor and generalize my existing objects
# autologin for mysql
# send cat5e through wall and into bedroom
# study object oriented perl
# write util docs


normalize file, so it can normailize a single file, and an array.